{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --filter '!@workspace/mockup-sandbox' --if-present run build",
    "test": "pnpm --filter ./workers/edge-proxy run test --run && pnpm --filter @workspace/syrabit run test",
    "deploy:pages": "pnpm dlx wrangler@4 pages deploy artifacts/syrabit/dist --project-name=${CF_PAGES_PROJECT_NAME:-syrabit} --branch=${CF_PAGES_BRANCH:-main} --commit-dirty=true",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "verify": "pnpm run typecheck && pnpm -r --if-present run verify:jsonld"
  },
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "devDependencies": {
    "prettier": "^3.8.1",
    "puppeteer": "^24.41.0",
    "typescript": "~5.9.2"
  },
  "pnpm": {
    "overrides": {
      "picomatch@2": "2.3.2",
      "picomatch@4": "4.0.4",
      "path-to-regexp": "8.4.0"
    }
  }
}
