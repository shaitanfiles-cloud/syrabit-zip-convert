 Pending Cloudflare Pages publish
These are fully written and committed but only take effect after artifacts/syrabit/dist/ is uploaded to Cloudflare Pages:

Path	What happens after deploy
/robots.txt	Pages worker proxies to backend's dynamic serve_robots_txt() instead of the deleted static file
/.well-known/ai-plugin.json	Pages worker proxies to backend's serve_ai_plugin_json()
/<INDEXNOW_KEY>.txt (32-hex or named variant)	Pages worker proxies to backend's serve_indexnow_key_root()
/feed.xml, /feed/*.xml, /llms.txt, /llms-full.txt	Already worked since #641, now also get proper _headers cache TTLs
Updated SW with SW_BYPASS_PATTERNS	Returning visitors stop seeing stale SW-cached sitemaps
Note: the backend's own /robots.txt and /.well-known/ai-plugin.json responses look like text/html when curled at localhost:8000 here, but that's because port 8000 is the local wrangler edge proxy, which doesn't handle root SEO paths — only the Pages worker does, and the Pages worker only runs on Cloudflare. The backend definitions themselves are correct (Response with proper media_type at server.py:1126 and server.py:841).

⚠️ Things I deliberately did NOT do (worth flagging)
sw.js CACHE_VERSION was NOT bumped (still '13'). The new bypass logic will activate on the next visit because the file bytes changed (browsers re-parse the SW on byte change), but bumping to '14' would force-evict any stale entries already in the SW cache for these paths. Low-risk to skip, but worth a one-liner bump on the next deploy if you want belt-and-suspenders cleanup.
Trailing-slash prefix matching in tryD1Route (audit item #10): left as-is, no concrete bug today. Already filed as a tech-debt follow-up.
Three audit items were false positives and intentionally skipped:
getSubjectsByCourseType helper — backend route exists at routes/content.py:313
RSS/Atom <link rel="alternate"> discovery tags — already in index.html
Trailing-slash prefix collision — see above
🚀 To make the pending items live
Publish the project to Cloudflare Pages — the new dist/_worker.js, _headers, _routes.json, and sw.js will all activate together. After publishing, the verification curls in follow-up task #647-area should all return 200 with the correct Content-Type from the backend.