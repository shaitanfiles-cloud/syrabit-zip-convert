Phase 2: Content & QA (Week 2, 5 steps)
Focus: Log chats → curate QA → display on pages → admin review loop.

1. Chat Logging (backend/chat.py or ai_handler.py)
python
# Add to your chat endpoint
chat_log = {
    "session_id": str(uuid.uuid4()),
    "user_id": user_id or "anon",
    "question": user_question,
    "raw_ai_answer": llm_response,
    "linked_doc_ids": rag_results.doc_ids if rag_results else [],
    "board": inferred_board,
    "timestamp": datetime.utcnow()
}
db.chat_messages.insert_one(chat_log)
2. QA Pairs DB (backend/db_models.py)
python
qa_schema = {
    "id": ObjectId(),
    "library_doc_id": doc_id,  # links to /ahsec/class-11/maths/limits
    "user_question": question,
    "ai_answer": answer,
    "language": "en",  # or "as", "hi"
    "is_published": False,
    "upvotes": 0,
    "views": 0,
    "published_at": None
}
db.qa_pairs.create_index("library_doc_id")
3. Admin QA Review (frontend/src/AdminQaManager.jsx)
jsx
// New tab in AdminSeoManager
const TABS = [...existing, { id: "qa", label: "QA Review" }];

<AdminQaTab 
  qaPairs={qaPairs}  // /api/admin/qa?doc_id=...
  onPublish={(qaId) => adminPublishQa(qaId)}
/>
4. Display QA on Topic Pages (SeoTopicPage.jsx)
jsx
// Bottom of page
<CommonQuestions 
  qaPairs={topicQaPairs.slice(0, 20)}  // from /api/seo/page/... +qa
  onViewMore={() => navigate(`/ahsec/class-11/maths/limits/qa`)}
/>
5. Backend Endpoints (seo_engine.py)
python
@app.route("/api/seo/page/<board>/<class_slug>/<subject_slug>/<topic_slug>", methods=["GET"])
def get_seo_page(...):
    page_data = _generate_single_page(...)
    page_data["qa_pairs"] = get_published_qa(doc_id)[:20]
    return page_data

@app.route("/api/admin/qa", methods=["GET", "POST"])
@admin_required
def admin_qa():
    if GET: return list_qa_by_doc(request.args.get("doc_id"))
    if POST: return publish_qa(request.json["qa_id"])
Deploy & Test (15 mins)
text
1. Backend: Add chat_log + qa_pairs + endpoints → restart uvicorn
2. Frontend: AdminQaTab + CommonQuestions → npm run dev
3. Test: Chat "limits class 11" → check DB log → admin review/publish → topic page shows QA
4. Render/Netlify deploy
Phase 2 Milestones
text
✅ Chat logs saved (100% capture)
✅ 50 QA pairs curated/published 
✅ 10 topics show "Common Questions"
✅ QA feeds RAG (Phase 4 upgrade)
Run pilot first: Admin → SEO → Pilot (AHSEC 11 Maths, 3 chapters) → publish → test chat logging on new pages.