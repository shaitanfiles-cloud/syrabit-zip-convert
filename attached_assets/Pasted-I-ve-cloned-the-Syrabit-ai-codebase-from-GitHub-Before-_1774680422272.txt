I've cloned the Syrabit.ai codebase from GitHub. Before writing a single line of code, do a complete deep analysis of the entire repository — every file, every feature, every logic flow, every API route, every component, every database schema, every environment variable — and produce a full understanding of what exists today.
First, run the setup:
pnpm install
pip install -r artifacts/syrabit-backend/requirements.txt
cp artifacts/syrabit-backend/.env.example artifacts/syrabit-backend/.env
cp artifacts/syrabit/.env.example artifacts/syrabit/.env
Then ask me to fill in the secrets before starting any server.
Codebase structure:
Backend: artifacts/syrabit-backend/server.py (FastAPI + MongoDB + Motor)
Frontend: artifacts/syrabit/src/ (React + Vite, pnpm monorepo)
SEO engine: artifacts/syrabit-backend/seo_engine.py
QA engine: artifacts/syrabit-backend/qa_engine.py
Prompts: artifacts/syrabit-backend/prompts.py
Models: artifacts/syrabit-backend/models.py
All env vars documented in .env.example files in each folder
After analysis and confirming the app boots and runs, build the enterprise version in these exact phases — each phase must be fully working and tested before starting the next:
Phase 1 — Foundation: Proper project structure, Docker-ready layout, environment validation on startup (fail fast if required vars are missing), structured JSON logging (replace all print statements), global exception handler with consistent error response shape, health-check + readiness endpoints, production-grade CORS locked to real domains.
Phase 2 — Database & Caching: All MongoDB collections with compound indexes, connection pooling tuned for production, Redis caching layer for search results, session data, and frequent queries, graceful degradation when Redis is unavailable, slow-query logging.
Phase 3 — Auth & Security: JWT hardening (access + refresh token pair, configurable expiry), RBAC middleware (admin / student / guest roles enforced on every protected route), rate limiting on all endpoints, input sanitization with Pydantic schemas on every request and response, security headers (HSTS, CSP, X-Frame-Options, X-Content-Type).
Phase 4 — API Layer: Clean versioned REST API under /api/v1/, Pydantic request/response models on every endpoint, OpenAPI docs auto-generated at /docs, streaming chat with proper backpressure, public search endpoint, verify every endpoint with curl examples before moving on.
Phase 5 — Performance: Redis page cache for SEO topic pages (TTL-based), MongoDB aggregation pipeline optimization, LLM response caching for repeated identical questions, Gzip middleware for all responses, connection pool tuning, measure baseline response times before and after.
Phase 6 — Admin Dashboard: Full RBAC-protected admin panel, bulk SEO topic generation (100 topics at once with progress tracking), QA curation workflow, content analytics (questions per subject, credit usage, top topics, page views), CSV and JSON export for all data.
Phase 7 — Monetization: Razorpay + Stripe dual payment integration, webhook handlers for payment events, subscription plan enforcement middleware, usage tracking per user, free tier (10 chats) → paid tier auto-enforcement, credit top-up flow.
Phase 8 — CI/CD & Production: Dockerfile + docker-compose.yml (FastAPI + Vite build), GitHub Actions pipeline (lint → test → build → deploy), pytest suite covering all API routes, Playwright e2e tests for critical flows (signup → chat → library → payment), Render/Railway deployment config with health checks.
Rules — non-negotiable:
Never mock data — use the real MongoDB collections from day one
Each phase ends with a live working demo verified by you before proceeding
Preserve every existing feature: streaming chat, library, curriculum map, SEO topic pages, admin panel, exam routines, credit system, QA review, library search sources panel
Never drop these collections: chat_messages, qa_pairs, seo_pages, chunks, subjects, chapters, users, analytics
Install all dependencies, restart all workflows, and confirm every endpoint responds correctly before declaring a phase complete
Commit after each phase with a meaningful message
Start now: Run the setup commands, ask me for the env secrets, boot the server, confirm it's healthy, then give me the full codebase analysis. No code changes until the analysis is done.