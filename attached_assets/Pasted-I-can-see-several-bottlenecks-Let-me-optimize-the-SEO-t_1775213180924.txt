I can see several bottlenecks. Let me optimize the SEO topic page for maximum speed:

Prioritize main content — render article as soon as notes API returns, defer everything else
Cache API responses — SEO content is static, use aggressive caching
Prefetch the page chunk — so JS is ready when link is clicked
Lazy load below-fold sections — flashcards, mnemonics, IQ load after first paint
Backend cache headers — tell browser to cache SEO page data
Defer non-critical API calls — related topics and page types can load after content shows
Now I have the full picture. Here's my optimization plan:

Frontend:

Split the data fetching — render the article immediately when main content arrives, defer secondary data
Lazy-load the below-fold sections (flashcards, mnemonics, IQ)
Add SeoTopicPage to prefetch list
Cache SEO API responses in-memory
Backend:
5. Add Cache-Control headers to SEO endpoints (static content, safe to cache)

Create a combined endpoint that returns page + pageTypes + related in one request to eliminate waterfall
Let me implement all of these:

Now I have enough context. Let me implement all the optimizations:

Step 1: Create a combined backend endpoint that returns page + pageTypes + related in a single API call

Now let me implement all optimizations. I'll create a combined backend endpoint and rewrite the frontend data fetching:

Now I need to remove the old duplicate get_seo_page_default that was before the bundle endpoint. Let me check:

There's a duplicate route now. I need to remove the original one (line 1810-1825) since the new one below is identical but with cache headers:

Also add cache headers to page-types and related endpoints:

Now let me add the frontend API function for the bundle endpoint and rewrite the SeoTopicPage data fetching:

Now rewrite the SeoTopicPage data fetching to use the bundle endpoint (1 request instead of 3), and defer the related topics fetch:

Now rewrite the data fetching to use the bundle endpoint — this is the big win, going from 3 API calls to 1:

Now let me add the SeoTopicPage to the prefetch list so the JS chunk is ready before the user clicks a share link. Also add prefetching for the page chunk in pageImports.js:

Now use the prefetch for the SeoTopicPage lazy import in App.jsx:

Now restart the backend to pick up the new endpoint:

Let me test the bundle endpoint works:

Bundle endpoint works — returning all data in a single response. Now let me verify the cache headers:

Cache-Control header is working: public, max-age=3600, s-maxage=86400, stale-while-revalidate=86400. This means:

Browser caches for 1 hour
CDN/proxy caches for 24 hours
Stale content served for 24 hours while revalidating in background
Now let me verify the page still loads correctly with the new bundle approach:

Page loads correctly with the bundle endpoint. Now let me run the e2e test:

SEO Topic Page Speed and Content Check
